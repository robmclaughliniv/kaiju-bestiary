{
  "name": "kaiju-bestiary-api",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "package": "npm run build && node scripts/zip.mjs"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.750.0",
    "@aws-sdk/lib-dynamodb": "^3.750.0"
  },
  "devDependencies": {
    "esbuild": "^0.25.0"
  }
}
